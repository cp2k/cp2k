diff --git a/CMakeLists.txt b/CMakeLists.txt
index 787e2ce..40bf714 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -141,6 +141,15 @@ find_package(SpFFT 1.0.2 CONFIG REQUIRED)
 find_package(SPLA 1.4.0 CONFIG REQUIRED)
 
+# Fix for HPC SDK CUDA targets
+if(TARGET SPLA::spla AND NOT TARGET CUDA::cudart)
+  find_library(CUDA_CUDART_LIBRARY NAMES cudart PATHS "$ENV{CUDA_HOME}/lib64" "/opt/nvidia/hpc_sdk/Linux_x86_64/25.9/cuda/13.0/targets/x86_64-linux/lib" "/usr/local/cuda/lib64")
+  if(CUDA_CUDART_LIBRARY)
+    add_library(CUDA::cudart INTERFACE IMPORTED)
+    set_property(TARGET CUDA::cudart PROPERTY INTERFACE_LINK_LIBRARIES "${CUDA_CUDART_LIBRARY}")
+  endif()
+endif()
+
 if (SIRIUS_USE_PUGIXML)
    find_package(pugixml REQUIRED)
 endif()
