# runs are executed in the same order as in this file
# the second field tells which test should be run in order to compare with the last available output
# e.g. 0 means do not compare anything, running is enough
#      1 compares the last total energy in the file
#      for details see cp2k/tools/do_regtest
#
# compute ground state for graphene with PBE and SZV
# compute the floquet DOS and QUASI_ENERGIES at K-point
"Floquet_KP.inp"  = [{matcher="Quasienergy", file="QUASI_ENERGIES.bs", tol=1.0E-06, ref=0.00534452},
                     {matcher="Floquet_DOS", file="FLOQUET_DOS.out",   tol=1e-8,  ref=24.91}]
#EOF
